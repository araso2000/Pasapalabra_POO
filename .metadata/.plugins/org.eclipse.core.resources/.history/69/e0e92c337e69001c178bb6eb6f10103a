package pasapalabra;

import java.util.Comparator;

public class ordenarGanadas implements Comparator <Jugador>{

	@Override
	public int compare(Jugador o1, Jugador o2) {
		if(o1.getPuntos()>o2.getPuntos()){
            return 0;
        }else{
            return 1;
        }
	}
}
